<header class="header">
    <div class="logo-box">
        <img class="logo" src="images/newspaper.gif" alt="Logo">
    </div>
    <div class="text-box">
        <h1 class="heading-primary">
          <span class="heading-primary-main">L.A. Times</span>
          <span class="heading-primary-sub">Get the Latest News</span>
        </h1>
        <a id="scrape" class="btn btn-white btn-animated" href="{{{route}}}">{{btn}}</a>
    </div>
</header>
<div class="container">
    <div class="card-deck mt-3">
        <div class="message text-center">
          <h2>{{message}}</h2>
        </div>
    </div>
</div>